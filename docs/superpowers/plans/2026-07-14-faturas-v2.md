# Faturas v2 — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Unificar Cartões dentro de Faturas com visão mensal (hero estilo Nubank), pagamento por parcela (`invoice_payments`) gerando despesa automática com a categoria da fatura, seção "Faturas encerradas" e widget "Faturas do mês" no Início — web e mobile.

**Architecture:** Nova tabela filha `invoice_payments` (espelho de `charge_payments`, com `installment_number` único por fatura) vira a fonte da verdade; `invoices` ganha `category_id` e `first_due_date` (âncora do cronograma mensal) e perde `due_date`. Toda derivação (vencimento por parcela, atraso, resumo do mês) é função pura em `@pmf/core`, compartilhada web/mobile — `invoices.list` retorna cronograma + pagamentos e **não há endpoints de agregação**. Visual canônico: `docs/superpowers/specs/2026-07-14-faturas-v2-mockup.html` (abrir no navegador; portar markup/estados de lá).

**Tech Stack:** Drizzle/Postgres, tRPC/Fastify, Zod (`@pmf/schemas`), React/Next.js (`@pmf/ui-web`), Expo/NativeWind, Vitest (+pglite na API).

**Spec:** `docs/superpowers/specs/2026-07-14-faturas-v2-design.md`.

## Global Constraints

- Máx. **300 linhas por arquivo**; lógica de negócio pura em `packages/core`; dinheiro `numeric(12,2)` e matemática em **centavos**; toda query/mutação filtra/valida `user_id`.
- Código/tabelas EN; UI, commits e docs PT-BR.
- Services **nunca** importam `TRPCError` — sentinelas (`null` → NOT_FOUND, `'conflict'` → CONFLICT, `'invalid_installment'` → BAD_REQUEST) mapeadas no router (padrão 8.1).
- **Git:** commits em PT-BR ao fim de cada tarefa, na branch `feat/fase8-cobrancas-cartoes`. **Não dar push sem o Hugo pedir.** (O working tree já tem 8.1+8.2 não commitados — o Hugo autorizou commits por tarefa deste plano? Se não autorizou explicitamente na execução, PULAR os passos de commit e apenas acumular no working tree.)
- Gates finais: `pnpm lint && pnpm typecheck && pnpm test && pnpm build`.
- Datas "hoje" sempre injetadas como parâmetro (`today: string` ISO) nas funções core — nunca `new Date()` dentro de `packages/core`.

---

### Task 1: Core — cronograma e estados de parcela (`invoice-installments.ts`)

**Files:**
- Create: `packages/core/src/invoice-installments.ts`
- Create: `packages/core/src/invoice-installments.test.ts`
- Modify: `packages/core/src/index.ts` (adicionar `export * from './invoice-installments'`)

**Interfaces:**
- Consumes: nada (módulo puro).
- Produces (usado pelas Tasks 4–9):

```ts
export interface InvoiceSchedule {
  amountPerInstallment: number
  totalInstallments: number
  firstDueDate: string // 'YYYY-MM-DD'
}
export interface InstallmentPayment {
  id: string
  installmentNumber: number
  amount: number
  paidOn: string // 'YYYY-MM-DD'
}
export interface InstallmentState {
  number: number
  dueDate: string
  paid: boolean
  overdue: boolean          // !paid && dueDate < today
  amountPaid: number | null // valor do pagamento, se pago
  paymentId: string | null
}
export interface InvoiceMonthSummary {
  due: number      // soma das parcelas NÃO pagas que vencem no mês
  paidAmt: number  // soma paga das parcelas do mês
  overdue: number  // soma das parcelas do mês em atraso
  nPend: number
  nPaid: number
  total: number    // due + paidAmt
}

export function installmentDueDate(firstDueDate: string, n: number): string
export function installmentForMonth(schedule: InvoiceSchedule, month: string /* 'YYYY-MM' */): number | null
export function deriveInstallments(schedule: InvoiceSchedule, payments: InstallmentPayment[], today: string): InstallmentState[]
export function invoiceMonthSummary(
  rows: Array<{ schedule: InvoiceSchedule; payments: InstallmentPayment[] }>,
  month: string,
  today: string,
): InvoiceMonthSummary
export function isInvoiceClosed(schedule: InvoiceSchedule, payments: InstallmentPayment[]): boolean
```

- [ ] **Step 1: Escrever os testes (falhando)** — `packages/core/src/invoice-installments.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import {
  deriveInstallments,
  installmentDueDate,
  installmentForMonth,
  invoiceMonthSummary,
  isInvoiceClosed,
} from './invoice-installments'

const sched = { amountPerInstallment: 250, totalInstallments: 10, firstDueDate: '2026-04-05' }
const pay = (n: number, amount = 250, paidOn = '2026-07-01', id = `p${n}`) => ({
  id, installmentNumber: n, amount, paidOn,
})

describe('installmentDueDate', () => {
  it('parcela 1 = primeiro vencimento', () => {
    expect(installmentDueDate('2026-04-05', 1)).toBe('2026-04-05')
  })
  it('soma meses e vira o ano', () => {
    expect(installmentDueDate('2026-11-15', 3)).toBe('2027-01-15')
  })
  it('clampa fim de mês (31 jan → 28 fev)', () => {
    expect(installmentDueDate('2026-01-31', 2)).toBe('2026-02-28')
  })
  it('clampa fev em ano bissexto', () => {
    expect(installmentDueDate('2027-12-31', 3)).toBe('2028-02-29')
  })
})

describe('installmentForMonth', () => {
  it('acha a parcela do mês', () => {
    expect(installmentForMonth(sched, '2026-07')).toBe(4)
  })
  it('null fora do cronograma', () => {
    expect(installmentForMonth(sched, '2026-03')).toBeNull()
    expect(installmentForMonth(sched, '2027-02')).toBeNull()
  })
})

describe('deriveInstallments', () => {
  it('marca pagas, atrasadas e pendentes', () => {
    const st = deriveInstallments(sched, [pay(1), pay(2)], '2026-07-14')
    expect(st).toHaveLength(10)
    expect(st[0]).toMatchObject({ number: 1, paid: true, amountPaid: 250, paymentId: 'p1', overdue: false })
    expect(st[2]).toMatchObject({ number: 3, paid: false, overdue: true, dueDate: '2026-06-05' }) // venceu, não paga
    expect(st[3]).toMatchObject({ number: 4, paid: false, overdue: true, dueDate: '2026-07-05' }) // venceu no mês atual
    expect(st[4]).toMatchObject({ number: 5, paid: false, overdue: false })
  })
  it('permite pagamento fora de ordem', () => {
    const st = deriveInstallments(sched, [pay(3)], '2026-04-01')
    expect(st[2]!.paid).toBe(true)
    expect(st[0]!.paid).toBe(false)
  })
})

describe('invoiceMonthSummary', () => {
  it('soma em centavos sem erro de float', () => {
    const rows = [
      { schedule: { amountPerInstallment: 0.1, totalInstallments: 3, firstDueDate: '2026-07-01' }, payments: [] },
      { schedule: { amountPerInstallment: 0.2, totalInstallments: 3, firstDueDate: '2026-07-02' }, payments: [pay(1, 0.2)] },
    ]
    const s = invoiceMonthSummary(rows, '2026-07', '2026-06-30')
    expect(s).toEqual({ due: 0.1, paidAmt: 0.2, overdue: 0, nPend: 1, nPaid: 1, total: 0.3 })
  })
  it('conta atraso apenas de parcela vencida e não paga', () => {
    const s = invoiceMonthSummary([{ schedule: sched, payments: [] }], '2026-07', '2026-07-14')
    expect(s).toMatchObject({ due: 250, overdue: 250, nPend: 1, nPaid: 0 })
  })
})

describe('isInvoiceClosed', () => {
  it('fechada quando toda parcela tem pagamento', () => {
    const p = [1, 2, 3].map((n) => pay(n))
    expect(isInvoiceClosed({ ...sched, totalInstallments: 3 }, p)).toBe(true)
    expect(isInvoiceClosed({ ...sched, totalInstallments: 4 }, p)).toBe(false)
  })
})
```

- [ ] **Step 2: Rodar e ver falhar** — `pnpm --filter @pmf/core test -- invoice-installments` → FAIL (módulo não existe).
- [ ] **Step 3: Implementar** — `packages/core/src/invoice-installments.ts` (as interfaces acima + implementação; datas via aritmética UTC de string, sem `new Date()` local para evitar timezone):

```ts
/** Cronograma e estados de parcela de fatura (Faturas v2). Datas ISO 'YYYY-MM-DD'. */
// (interfaces InvoiceSchedule, InstallmentPayment, InstallmentState, InvoiceMonthSummary — ver bloco acima)

const cents = (v: number) => Math.round(v * 100)

/** Vencimento da parcela n (1-based): firstDueDate + (n-1) meses, com clamp de fim de mês. */
export function installmentDueDate(firstDueDate: string, n: number): string {
  const [y, m, d] = firstDueDate.split('-').map(Number)
  const monthIndex = m! - 1 + (n - 1)
  const year = y! + Math.floor(monthIndex / 12)
  const month = monthIndex % 12
  const lastDay = new Date(Date.UTC(year, month + 1, 0)).getUTCDate()
  const day = Math.min(d!, lastDay)
  return `${year}-${String(month + 1).padStart(2, '0')}-${String(day).padStart(2, '0')}`
}

export function installmentForMonth(schedule: InvoiceSchedule, month: string): number | null {
  for (let n = 1; n <= schedule.totalInstallments; n++) {
    const due = installmentDueDate(schedule.firstDueDate, n).slice(0, 7)
    if (due === month) return n
    if (due > month) return null
  }
  return null
}

export function deriveInstallments(
  schedule: InvoiceSchedule,
  payments: InstallmentPayment[],
  today: string,
): InstallmentState[] {
  const byNumber = new Map(payments.map((p) => [p.installmentNumber, p]))
  return Array.from({ length: schedule.totalInstallments }, (_, i) => {
    const number = i + 1
    const dueDate = installmentDueDate(schedule.firstDueDate, number)
    const payment = byNumber.get(number)
    return {
      number,
      dueDate,
      paid: !!payment,
      overdue: !payment && dueDate < today,
      amountPaid: payment ? payment.amount : null,
      paymentId: payment ? payment.id : null,
    }
  })
}

export function invoiceMonthSummary(
  rows: Array<{ schedule: InvoiceSchedule; payments: InstallmentPayment[] }>,
  month: string,
  today: string,
): InvoiceMonthSummary {
  let due = 0, paidAmt = 0, overdue = 0, nPend = 0, nPaid = 0
  for (const { schedule, payments } of rows) {
    const n = installmentForMonth(schedule, month)
    if (!n) continue
    const st = deriveInstallments(schedule, payments, today)[n - 1]!
    if (st.paid) { paidAmt += cents(st.amountPaid!); nPaid++ }
    else {
      due += cents(schedule.amountPerInstallment); nPend++
      if (st.overdue) overdue += cents(schedule.amountPerInstallment)
    }
  }
  return { due: due / 100, paidAmt: paidAmt / 100, overdue: overdue / 100, nPend, nPaid, total: (due + paidAmt) / 100 }
}

export function isInvoiceClosed(schedule: InvoiceSchedule, payments: InstallmentPayment[]): boolean {
  const nums = new Set(payments.map((p) => p.installmentNumber))
  for (let n = 1; n <= schedule.totalInstallments; n++) if (!nums.has(n)) return false
  return true
}
```

- [ ] **Step 4: Rodar e ver passar** — `pnpm --filter @pmf/core test -- invoice-installments` → PASS. Adicionar o export em `packages/core/src/index.ts` e rodar `pnpm --filter @pmf/core typecheck` (script `typecheck` do pacote; se não existir, `pnpm typecheck` na raiz).
- [ ] **Step 5: Commit** (se autorizado): `git add packages/core && git commit -m "feat(core): cronograma e estados de parcela de fatura (faturas v2)"`

---

### Task 2: Banco — `invoice_payments`, `category_id`, `first_due_date`, enum `invoice`

**Files:**
- Modify: `apps/api/src/db/schema/enums.ts` (txSource ganha `'invoice'`)
- Modify: `apps/api/src/db/schema/installment-accounts.ts`
- Modify: `apps/api/src/test/test-db.ts` (DDL espelhada do pglite)
- Create (gerado): `apps/api/drizzle/0003_*.sql` (+ snapshot)

**Interfaces:**
- Produces: tabela `invoicePayments` exportada pelo schema (via `apps/api/src/db/schema/index.ts`, que já reexporta `installment-accounts`); colunas novas em `invoices` (`categoryId`, `firstDueDate`; sem `dueDate`).

- [ ] **Step 1: Editar o schema Drizzle** — em `installment-accounts.ts`, no `invoices`: adicionar

```ts
categoryId: uuid('category_id').references(() => categories.id, { onDelete: 'set null' }),
firstDueDate: date('first_due_date').notNull(),
```

remover a linha `dueDate: date('due_date'),` **apenas do `invoices`** (o `charges` mantém a dele), importar `categories` de `./categories`, e adicionar ao fim do arquivo:

```ts
// Pagamento de parcela de fatura: 1 row por parcela paga + FK da despesa gerada (faturas v2),
// espelhando charge_payments
export const invoicePayments = pgTable(
  'invoice_payments',
  {
    id: uuid('id').primaryKey().defaultRandom(),
    userId: text('user_id')
      .notNull()
      .references(() => users.id, { onDelete: 'cascade' }),
    invoiceId: uuid('invoice_id')
      .notNull()
      .references(() => invoices.id, { onDelete: 'cascade' }),
    installmentNumber: integer('installment_number').notNull(),
    amount: numeric('amount', { precision: 12, scale: 2 }).notNull(),
    paidOn: date('paid_on').notNull(),
    transactionId: uuid('transaction_id').references(() => transactions.id, {
      onDelete: 'set null',
    }),
    createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
  },
  (t) => [
    unique('invoice_payment_installment_unique').on(t.invoiceId, t.installmentNumber),
    check('invoice_payment_installment_min', sql`${t.installmentNumber} >= 1`),
  ],
)
```

(importar `unique` de `drizzle-orm/pg-core`). Em `enums.ts`, adicionar `'invoice'` ao array do `txSource`.

- [ ] **Step 2: Gerar a migration** — `cd apps/api && pnpm db:generate`. Inspecionar o SQL gerado.
- [ ] **Step 3: Editar o SQL gerado para o backfill** — o Drizzle vai gerar `ADD COLUMN "first_due_date" date NOT NULL` (quebra com rows existentes) e `DROP COLUMN "due_date"`. Reordenar/editar manualmente para:

```sql
ALTER TABLE "invoices" ADD COLUMN "first_due_date" date;--> statement-breakpoint
UPDATE "invoices" SET "first_due_date" = COALESCE("due_date", "created_at"::date);--> statement-breakpoint
ALTER TABLE "invoices" ALTER COLUMN "first_due_date" SET NOT NULL;--> statement-breakpoint
ALTER TABLE "invoices" DROP COLUMN "due_date";
```

mantendo o restante (enum, category_id, invoice_payments) como gerado.

- [ ] **Step 4: Aplicar no Postgres local** — `docker start pmf-db` (se parado) e `pnpm db:migrate`. Verificar: `docker exec pmf-db psql -U postgres -d pmf -c '\d invoice_payments'` → tabela com unique `(invoice_id, installment_number)`.
- [ ] **Step 5: Espelhar no pglite** — em `apps/api/src/test/test-db.ts`: adicionar `'invoice'` no enum `tx_source`, colunas `category_id uuid references categories(id) on delete set null` e `first_due_date date not null` em `invoices` (removendo `due_date` de `invoices`), e a DDL de `invoice_payments` com o unique. Rodar `pnpm --filter @pmf/api test` → suites existentes de invoices vão falhar (usam `dueDate`) — **esperado**; serão corrigidas na Task 4.
- [ ] **Step 6: Commit** (se autorizado): `git add apps/api/src/db apps/api/drizzle apps/api/src/test/test-db.ts && git commit -m "feat(api): schema faturas v2 — invoice_payments, categoria e primeiro vencimento"`

---

### Task 3: Schemas Zod (`@pmf/schemas`) + draft de fatura no core

**Files:**
- Modify: `packages/schemas/src/installment-accounts.ts`
- Modify: `packages/core/src/installments.ts` (draft específico de fatura) e `packages/core/src/index.ts` se necessário

**Interfaces:**
- Produces (Tasks 4–8 consomem):

```ts
// @pmf/schemas
export const createInvoiceInput   // { cardName, cardId?, description?, amountPerInstallment, totalInstallments, firstDueDate, categoryId?, status? } — SEM amountPaid/dueDate
export const updateInvoiceInput   // idem + id; cardId/categoryId/description nullable
export const registerInvoicePaymentInput   // { id: uuid, installmentNumber: int>=1, amount: money, paidOn: isoDate }
export const updateInvoicePaymentInput     // { paymentId: uuid, amount: money, paidOn: isoDate }
export const unregisterInvoicePaymentInput // { paymentId: uuid }

// @pmf/core (installments.ts)
export interface InvoiceDraft { name: string; description: string; amountPerInstallment: string; totalInstallments: string; firstDueDate: string; categoryId: string; cardId: string }
export const emptyInvoiceDraft: InvoiceDraft
export function validateInvoiceDraft(d: InvoiceDraft): string | null
export function parseInvoiceDraft(d: InvoiceDraft): { description?: string; amountPerInstallment: number; totalInstallments: number; firstDueDate: string; categoryId?: string; cardId?: string }
```

- [ ] **Step 1: Reescrever os inputs de invoice** em `packages/schemas/src/installment-accounts.ts` — invoices deixam de usar `installmentBase` (que carrega `amountPaid`/`dueDate`, ainda usados por charges):

```ts
const invoiceBase = z.object({
  cardName: z.string().min(1).max(120),
  description: z.string().max(500).optional(),
  amountPerInstallment: money,
  totalInstallments: z.number().int().min(1),
  firstDueDate: isoDate,
  categoryId: uuid.optional(),
})

export const createInvoiceInput = invoiceBase.extend({
  cardId: uuid.optional(),
  status: z.enum(['pendente', 'pago']).default('pendente'),
})
export type CreateInvoiceInput = z.infer<typeof createInvoiceInput>

export const updateInvoiceInput = invoiceBase.extend({
  id: uuid,
  cardId: uuid.nullable().optional(),
  categoryId: uuid.nullable().optional(),
  description: z.string().max(500).nullable().optional(),
  status: z.enum(['pendente', 'pago']),
})
export type UpdateInvoiceInput = z.infer<typeof updateInvoiceInput>

export const registerInvoicePaymentInput = z.object({
  id: uuid,
  installmentNumber: z.number().int().min(1),
  amount: money,
  paidOn: isoDate,
})
export type RegisterInvoicePaymentInput = z.infer<typeof registerInvoicePaymentInput>

export const updateInvoicePaymentInput = z.object({ paymentId: uuid, amount: money, paidOn: isoDate })
export type UpdateInvoicePaymentInput = z.infer<typeof updateInvoicePaymentInput>

export const unregisterInvoicePaymentInput = z.object({ paymentId: uuid })
export type UnregisterInvoicePaymentInput = z.infer<typeof unregisterInvoicePaymentInput>
```

(`paidWithinTotal`/`paidRefine` continuam, agora usados só por charges. `setInvoiceStatusInput` permanece.)

- [ ] **Step 2: Draft de fatura no core** — em `packages/core/src/installments.ts` adicionar `InvoiceDraft`/`emptyInvoiceDraft`/`validateInvoiceDraft`/`parseInvoiceDraft` (mesmo estilo do `InstallmentDraft`; validação: parcela > 0, parcelas ≥ 1, `firstDueDate` obrigatória no formato ISO). `InstallmentDraft` atual fica para charges. Se o arquivo passar de 300 linhas, mover o bloco de fatura para `packages/core/src/invoice-draft.ts` + export no index.
- [ ] **Step 3: Typecheck do monorepo** — `pnpm typecheck` → vão sobrar erros APENAS em `apps/api` (service/router de invoices) e nos forms web/mobile que usam os inputs antigos; anotar e seguir (Tasks 4 e 6–8 os corrigem). Se aparecer erro em charges, a Task quebrou o base compartilhado — reverter.
- [ ] **Step 4: Commit** (se autorizado): `git add packages/schemas packages/core && git commit -m "feat(schemas,core): inputs de fatura v2 com categoria e primeiro vencimento"`

---

### Task 4: Service + router `invoices` (list enriquecido, create/update novos)

**Files:**
- Modify: `apps/api/src/services/invoices.ts`
- Modify: `apps/api/src/trpc/routers/invoices.ts` (remover `summary`)
- Create: `apps/api/src/services/invoices.test.ts` (se ainda não existir suite equivalente; senão atualizar a existente)

**Interfaces:**
- Consumes: schema Task 2, inputs Task 3.
- Produces:

```ts
// listInvoices retorna, por fatura:
// { id, cardName, cardId, description, amountPerInstallment, totalInstallments, amountPaid,
//   firstDueDate, categoryId, categoryName: string | null, status, createdAt,
//   payments: Array<{ id, installmentNumber, amount, paidOn }> }  // ordenado por installmentNumber
export async function listInvoices(db: DrizzleDB, userId: string): Promise<...>
export async function createInvoice(db, userId, input: CreateInvoiceInput)   // valida cardId E categoryId do usuário; null se não pertencem
export async function updateInvoice(db, userId, input: UpdateInvoiceInput)
```

- [ ] **Step 1: Testes primeiro** (pglite, estilo `cards.test.ts`): criar fatura com categoria própria → row com `firstDueDate` e `categoryId`; criar com `categoryId` de outro usuário → `null`; `list` traz `categoryName` e `payments: []`; update troca categoria para `null`. Rodar → FAIL.
- [ ] **Step 2: Implementar** — `createInvoice`/`updateInvoice`: trocar `dueDate` por `firstDueDate: input.firstDueDate`, gravar `categoryId: input.categoryId ?? null`, `amountPaid` não vem mais do input (create grava `'0'`; update não toca — agregado é recalculado pelos pagamentos na Task 5). Validar categoria com helper local (select em `categories` por `id`+`userId`; padrão `cardBelongsToUser`). `listInvoices`: leftJoin `categories` (name) e carregar pagamentos das faturas do usuário com um único `select` em `invoicePayments` filtrado por `userId`, agrupando em memória por `invoiceId`. Apagar `invoicesSummary` e a procedure `summary` do router; `create`/`update` no router lançam BAD_REQUEST 'Categoria não encontrada' / NOT_FOUND 'Cartão não encontrado' conforme sentinela (usar sentinelas distintas: `'card_not_found' | 'category_not_found'`).
- [ ] **Step 3: Consertar suites/testes existentes** que usavam `dueDate`/`amountPaid`/`summary` de invoices (test-db já migrado na Task 2). `pnpm --filter @pmf/api test` → PASS.
- [ ] **Step 4: Commit** (se autorizado): `git add apps/api && git commit -m "feat(api): faturas v2 — list com parcelas/categoria, create/update com primeiro vencimento"`

---

### Task 5: Service + procedures de pagamento de parcela (despesa automática)

**Files:**
- Create: `apps/api/src/services/invoice-payments.ts`
- Create: `apps/api/src/services/invoice-payments.test.ts`
- Modify: `apps/api/src/trpc/routers/invoices.ts`

**Interfaces:**
- Consumes: `invoicePayments`/`invoices`/`transactions` do schema; `installmentDueDate`, `isInvoiceClosed` de `@pmf/core`; inputs da Task 3.
- Produces:

```ts
export async function registerInvoicePayment(db, userId, input: RegisterInvoicePaymentInput)
// → row | null (fatura não é do user) | 'invalid_installment' (n > totalInstallments) | 'conflict' (parcela já paga)
export async function updateInvoicePayment(db, userId, input: UpdateInvoicePaymentInput)  // → row | null
export async function unregisterInvoicePayment(db, userId, input: UnregisterInvoicePaymentInput) // → row | null
```

Regras (dentro de `db.transaction`, padrão `charge-payments.ts`):
- **register:** busca fatura por `id`+`userId`; valida `1 <= installmentNumber <= totalInstallments`; CONFLICT se já existe pagamento da parcela. Insere despesa: `{ userId, type: 'despesa', value: amount, date: paidOn, description: \`${invoice.description || invoice.cardName} — parcela ${n}/${total}\`, categoryId: invoice.categoryId, cardId: invoice.cardId, source: 'invoice' }`. Insere `invoice_payments` com `transactionId`. Recalcula agregados: `amountPaid = soma(amount)` dos pagamentos, `status = 'pago'` se `isInvoiceClosed`.
- **update:** valida dono via `userId` do pagamento; atualiza `amount`/`paidOn` e a transação vinculada (`value`/`date`); recalcula `amountPaid`.
- **unregister:** apaga pagamento + transação vinculada (se houver); recalcula `amountPaid`; `status 'pago' → 'pendente'`.

- [ ] **Step 1: Testes primeiro** (pglite; helper de user/categoria como em `charge-payments.test.ts`): registrar → cria despesa com categoria/cartão/source corretos e `amountPaid` atualizado; registrar todas → `status 'pago'`; parcela duplicada → `'conflict'`; `installmentNumber` fora da faixa → `'invalid_installment'`; fatura de outro user → `null`; update muda valor e despesa junto; unregister apaga despesa, reverte agregados e status; pagamento fora de ordem (3 antes da 2) funciona. Rodar → FAIL.
- [ ] **Step 2: Implementar o service** conforme regras acima (sem TRPCError).
- [ ] **Step 3: Procedures** no `invoicesRouter`: `registerPayment`, `updatePayment`, `unregisterPayment` mapeando sentinelas → `CONFLICT 'Parcela já registrada como paga'`, `BAD_REQUEST 'Parcela inexistente nesta fatura'`, `NOT_FOUND`. Retornar `{ payment, transaction }` no register (a UI usa a transação no toast).
- [ ] **Step 4: Rodar** `pnpm --filter @pmf/api test` → PASS; `pnpm typecheck` limpo no backend.
- [ ] **Step 5: Commit** (se autorizado): `git add apps/api && git commit -m "feat(api): pagamento por parcela de fatura com despesa automática"`

---

### Task 6: Web — tela Faturas v2 + remoção de Cartões

**Files:**
- Create: `apps/web/src/components/card-strip.tsx`, `invoice-month-hero.tsx`, `installments-panel.tsx`, `installment-payment-dialog.tsx`, `closed-invoices.tsx`
- Modify: `apps/web/src/app/(app)/faturas/page.tsx` (reescrita), `apps/web/src/components/invoice-form-fields.tsx` (campos novos), `apps/web/src/components/invoice-table.tsx` (expander/badge/mini-barra), `apps/web/src/components/nav-items.tsx` (remover item Cartões), `apps/web/src/components/tx-badges.tsx` (source `'invoice'` → badge "fatura")
- Delete: `apps/web/src/app/(app)/cartoes/page.tsx`

**Interfaces:**
- Consumes: `trpc.invoices.list/create/update/delete/registerPayment/updatePayment/unregisterPayment`, `trpc.cards.*`, `trpc.categories.list`; core: `deriveInstallments`, `invoiceMonthSummary`, `installmentForMonth`, `isInvoiceClosed`, `emptyInvoiceDraft`/`validateInvoiceDraft`/`parseInvoiceDraft`, `bankPresetInfo`.
- Produces (contratos entre componentes):

```tsx
// card-strip.tsx — faixa-filtro; reusa CardFormDialog e BankLogo existentes
export function CardStrip(props: {
  selected: string | 'all'
  onSelect: (id: string | 'all') => void
  invoiceCount: number
}): JSX.Element

// invoice-month-hero.tsx — hero escuro + barras 12 meses (mês corrente −4/+7)
export function InvoiceMonthHero(props: {
  rows: Array<{ schedule: InvoiceSchedule; payments: InstallmentPayment[] }>
  month: string;                 // 'YYYY-MM' selecionado
  onMonthChange: (m: string) => void
  today: string
}): JSX.Element

// installments-panel.tsx — chips + tabela de parcelas de UMA fatura
export function InstallmentsPanel(props: {
  installments: InstallmentState[]
  onOpenPayment: (n: number) => void
}): JSX.Element

// installment-payment-dialog.tsx — dialog + toast; deriva alvo do cache (payingKey), NUNCA snapshot
export interface PaymentTarget { invoiceId: string; installmentNumber: number }
export function InstallmentPaymentDialog(props: {
  target: PaymentTarget | null
  onClose: () => void
}): JSX.Element   // internamente busca a fatura via trpc.invoices.list (cache) e invalida invoices+transactions+dashboard

// closed-invoices.tsx — seção recolhida
export function ClosedInvoices(props: { rows: InvoiceListRow[]; showCard: boolean }): JSX.Element
```

- [ ] **Step 1: Formulário** — `invoice-form-fields.tsx` migra para `InvoiceDraft`: perde "Já pago" e "Vencimento (opcional)", ganha "Primeiro vencimento" (obrigatório, date) e "Categoria (opcional)" (select de categorias tipo despesa). `faturas/page.tsx` envia `createInvoiceInput` novo.
- [ ] **Step 2: Componentes novos** portando o markup do mockup (`2026-07-14-faturas-v2-mockup.html` é a referência 1:1 — classes Tailwind equivalentes ao CSS de lá, tokens do tema em vez de hex): CardStrip (tile "Todos os cartões" primeiro, tiles com editar/excluir + "+ Cartão" abrindo `CardFormDialog`), InvoiceMonthHero (barras verde=pago/vermelho=atraso/neutro, valores via `invoiceMonthSummary` por mês, linha "⚠ N parcelas em atraso" global), InstallmentsPanel (chips numerados com estados paga/prox/atraso + tabela com "registrar pagamento"/"editar"), InstallmentPaymentDialog (status segmentado, valor **pré-preenchido com `amountPerInstallment`**, data default hoje, bloco verde "↳ Ao salvar, uma despesa de R$ X será criada… categoria **<da fatura>**", toast pós-sucesso com dados da transação retornada e "desfazer" → `unregisterPayment`), ClosedInvoices.
- [ ] **Step 3: Página** — `faturas/page.tsx` orquestra: estados `month` (default mês corrente), `cardFilter`, `expandedId`, `payingTarget`; grupos por cartão filtrados (`isInvoiceClosed` separa ativas/encerradas); badge do mês por fatura (`installmentForMonth` + estado). Manter < 300 linhas (a composição fica nos componentes). Remover `/cartoes/page.tsx` e o item `{ href: '/cartoes', label: 'Cartões' }` de `nav-items.tsx`; `tx-badges.tsx` ganha `'invoice'` → "fatura".
- [ ] **Step 4: Verificar** — `pnpm --filter @pmf/web build` (ou `pnpm build` na raiz) → verde; conferir que nenhum arquivo passou de 300 linhas (`wc -l` nos tocados).
- [ ] **Step 5: Commit** (se autorizado): `git add apps/web && git commit -m "feat(web): tela faturas v2 — cartões integrados, hero mensal, parcelas e encerradas"`

---

### Task 7: Mobile — tela Faturas v2 + remoção de Cartões

**Files:**
- Create: `apps/mobile/src/components/card-strip.tsx`, `invoice-month-hero.tsx`, `installments-panel.tsx`, `installment-payment-modal.tsx`, `closed-invoices.tsx`
- Modify: `apps/mobile/app/mais/faturas.tsx` (reescrita), `apps/mobile/src/components/invoice-form-modal.tsx` (campos novos), `invoice-card.tsx` (badge do mês, mini-barra, rodapé "ver N parcelas"), `apps/mobile/app/(tabs)/mais.tsx` (remover item Cartões), `apps/mobile/app/mais/_layout.tsx` (remover título cartoes), `apps/mobile/src/components/tx-row.tsx` (source `'invoice'` → "fatura")
- Delete: `apps/mobile/app/mais/cartoes.tsx`

**Interfaces:** mesmos contratos da Task 6 em React Native (chips compactos com logo redondo; hero com barras `Pressable`; parcelas em **linha única** `P. 4 · 05/07 · R$ 250,00 · paga · editar` com vencimento sempre visível; pagamento como bottom-sheet `Modal`; toast como overlay no rodapé com auto-dismiss ~5s e "desfazer"). Reusa `CardFormModal` e `BankLogo` existentes.

- [ ] **Step 1:** Formulário (`invoice-form-modal.tsx`): trocar campos como na web (Primeiro vencimento AAAA-MM-DD obrigatório; chips de categoria de despesa, padrão do `tx-form-modal.tsx`).
- [ ] **Step 2:** Componentes novos portando o frame mobile do mockup; `faturas.tsx` orquestra igual à web (month/filter/expanded/paying derivado do cache).
- [ ] **Step 3:** Remover `cartoes.tsx`, item do menu Mais e título no `_layout`; badge "fatura" no `tx-row.tsx`.
- [ ] **Step 4:** Verificar bundle Metro (receita `.claude/skills/verify/SKILL.md`): subir Metro, baixar bundle com `lazy=false`, grep por símbolo novo (ex. `InvoiceMonthHero`) → presente; typecheck raiz verde.
- [ ] **Step 5: Commit** (se autorizado): `git add apps/mobile && git commit -m "feat(mobile): tela faturas v2 — cartões integrados, hero mensal, parcelas e encerradas"`

---

### Task 8: Widget "Faturas do mês" no Início (web + mobile)

**Files:**
- Create: `apps/web/src/components/invoices-month-widget.tsx`, `apps/mobile/src/components/invoices-month-widget.tsx`
- Modify: `apps/web/src/app/(app)/page.tsx`, `apps/mobile/app/(tabs)/index.tsx` (inserir o widget na grade do Início — atenção: a tela Início foi mexida pelo agente da 8.5 em outra branch; integrar no que existe NA NOSSA branch e anotar conflito para o merge)

**Interfaces:**
- Consumes: `trpc.invoices.list` + `invoiceMonthSummary`/`deriveInstallments` (mês corrente, `today` local).
- Comportamento (mockup): título "FATURAS DE <MÊS>"; valor a pagar + nº de parcelas; barra de progresso "R$ X pagos de R$ Y no mês"; alerta vermelho "⚠ N parcelas em atraso · R$ Z" (global, qualquer mês); lista das parcelas não pagas com vencimento ≤ fim do mês corrente ordenada por vencimento (vencidas em vermelho, "venceu dd/mm"); link "ver todas →" para `/faturas` (web) / push `mais/faturas` (mobile); estado vazio "✓ Faturas do mês em dia"; loading = skeleton simples; se `list` vazio, não renderizar o widget.

- [ ] **Step 1:** Implementar os dois widgets (mesma derivação, JSX por plataforma).
- [ ] **Step 2:** Plugar nas telas de Início.
- [ ] **Step 3:** `pnpm build` + bundle Metro verdes.
- [ ] **Step 4: Commit** (se autorizado): `git add apps/web apps/mobile && git commit -m "feat: widget faturas do mês na tela início (web e mobile)"`

---

### Task 9: Gates finais, verificação em runtime e rastreio

**Files:**
- Modify: `run_tasks.md` (**re-ler antes de editar** — outros agentes escrevem nele)

- [ ] **Step 1: Gates** — na raiz: `pnpm lint && pnpm typecheck && pnpm test && pnpm build` → tudo verde.
- [ ] **Step 2: Runtime web** — subir API (`AUTH_BYPASS=true`) + web (porta 3005) e verificar no navegador: sidebar sem "Cartões"; `/faturas` com hero (clicar meses), faixa-filtro (Todos/cartão), criar fatura com categoria + primeiro vencimento, expandir parcelas, registrar pagamento (dialog pré-preenchido → toast "Despesa criada" → despesa aparece no Histórico com badge "fatura", categoria e cartão certos), desfazer pelo toast (despesa some, agregados voltam), fatura 100% paga migra para "Faturas encerradas"; widget no Início. Limpar dados de teste ao final.
- [ ] **Step 3: Runtime mobile** — bundle Metro compila com os módulos novos (grep de símbolos); teste de toque no Expo Go é do Hugo.
- [ ] **Step 4: Rastreio** — `run_tasks.md`: entrada de changelog com data ISO ("Faturas v2 — evolução da 8.2") citando spec/plan; atualizar memória do projeto se surgir decisão nova. **Não dar push; avisar o Hugo que está pronto para commit/PR se os commits não foram autorizados.**

---

## Self-review (feita)

- **Cobertura da spec:** decisões 1–10 → Tasks 6/7 (1,2,4,5,6,7,9), 1 (3,5 derivações), 5 (8), 8 (10); modelo de dados → Task 2; core → Task 1; API → Tasks 4–5; fora de escopo respeitado (sem valores por parcela na criação, sem juros, sem notificação).
- **Sem placeholders:** todo passo tem código ou comando concreto; UI referencia o mockup HTML como fonte 1:1 de markup/estados (arquivo commitável na specs/).
- **Consistência de tipos:** nomes conferidos entre tasks (`InvoiceSchedule`, `InstallmentPayment`, `deriveInstallments`, `registerInvoicePaymentInput`, sentinelas `'conflict' | 'invalid_installment' | 'card_not_found' | 'category_not_found'`).
- **Riscos anotados:** migration com backfill manual (Step 3 da Task 2); `run_tasks.md` e tela Início compartilhados com outros agentes (merge é do Hugo); suites existentes de invoices quebram entre Task 2 e Task 4 por design (janela curta, mesmo PR).
